# SkillSum

Read `AGENTS.md` first — it is the canonical rule set for this repo.

Project skills live in `.claude/skills/` (soft-arcade-ui, api-endpoint, frontend-feature, game-data, review). Use the matching skill before starting its task type.
